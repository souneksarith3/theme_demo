import 'package:flutter/material.dart';

class StudentUpdate extends StatelessWidget {
  const StudentUpdate({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Student Update"),
      ),
    );
  }
}